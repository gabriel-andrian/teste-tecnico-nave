import FormLogin from "../../components/form-login";
import { LoginContainer } from "./style";

const Login = () => {
  return (
    <LoginContainer>
      <FormLogin />
    </LoginContainer>
  );
};

export default Login;
